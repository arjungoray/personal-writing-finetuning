# Implementation Spec

The active implementation spec was supplied at the repository root as `implementation-spec.md`.

This app is being implemented in stacked, reviewable layers. The root spec file is intentionally not duplicated here yet so local edits to the source specification remain obvious in `git status`.
